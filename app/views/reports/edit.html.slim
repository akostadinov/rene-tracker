| = form_for(@report) do |f| - if @report.errors.any? #error_explanation h2 = pluralize(@report.errors.count, "error") | prohibited this report from being saved: ul - @report.errors.full_messages.each do |msg| li = msg .field = f.label :date br = f.text_field :date .field = f.label :rc br = f.text_field :rc .field = f.label :ec br = f.text_field :ec .field = f.label :rm br = f.text_field :rm .actions = f.submit 
